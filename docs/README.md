# Steps to creating product // More details.
### The MVP will only contain encryption, no GUI, plain entering in terminal

1. Create a scanner that scans for the password in the terminal.
2. Encrypt using AES (Secret Key to decrypt).
3. Store using JSON. (Prootyope, will use SQL later).
4. Make a system to retrieve the password.